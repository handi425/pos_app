enum PaymentMethod { cash, transfer }

enum DebtStatusView { open, closed }

enum DebtPaymentMethodView { cash, transfer }

enum CashMovementType { inFlow, outFlow }

enum CashReferenceType { sale, debtPayment, adjustment, purchase }
